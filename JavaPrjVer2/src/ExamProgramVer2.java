
//import opp.has_a.ExamConsole;
/*import opp.is_a.Exam;*/

//build path 변경 오류

/*is a
 * 재사용
 * 
 * 똑같은 이름의 함수를 정의=>기존의 함수보다 우선순위가 높아져서 새로정의한 함수를 사용
 * 
 * OOP(객체지향):캡슐화,상속,다형성
 * **자바는 참조형식의 함수(Exam)보다 객체형식의 함수호출*(오버라이드)을 우선으로 한다.
 *  오버라이드가 된 경우에만 호출가능
 *   
 * 
 * */

public class ExamProgramVer2 {
	/*
	 * public static void main(String[] args) { // Exam exam = new NewlecExam(1, 1,
	 * 1, 1); // NewlecExam exam = new NewlecExam(30,50,30,50); // exam.setKor(30);
	 * // exam.setEng(30); // exam.setMath(30); // exam.setCom(30);
	 * 
	 * System.out.println(exam.total());
	 * 
	 * System.out.println(exam.avg());
	 * 
	 * System.out.println();
	 * 
	 * }
	 */
}
